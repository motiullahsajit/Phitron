    // int n;
    // cin >> n;

    // vector<int> arr;
    // int x;
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> x;
    //     cout << "before" << arr.size() << endl;
    //     arr.push_back(x);
    //     cout << "after" << arr.size() << endl;
    // }

    // for (int i = 0; i < arr.size(); i++)
    //     cout << arr[i] << endl;

    // arr.erase(arr.begin());
    // arr.pop_back();
    // arr.insert(arr.begin() + 1, 6);

    // for (int i = 0; i < arr.size(); i++)
    //     cout << arr[i] << endl;